# YOLOv8 with CoordAtt and BiFPN
nc: 80  # number of classes
scales: # model compound scaling constants, i.e. 'model=yolov8n.yaml' will call yolov8.yaml with scale 'n'
  # [depth, width, max_channels]
  n: [0.33, 0.25, 1024]  # YOLOv8n summary: 225 layers,  3157200 parameters,  3157184 gradients,   8.9 GFLOPs
  # s: [0.33, 0.50, 1024]  # YOLOv8s summary: 225 layers, 11166560 parameters, 11166544 gradients,  28.8 GFLOPs
  # m: [0.67, 0.75, 768]   # YOLOv8m summary: 295 layers, 25902640 parameters, 25902624 gradients,  79.3 GFLOPs
  # l: [1.00, 1.00, 512]   # YOLOv8l summary: 365 layers, 43691520 parameters, 43691504 gradients, 165.7 GFLOPs
  # x: [1.00, 1.25, 512]   # YOLOv8x summary: 365 layers, 68229648 parameters, 68229632 gradients, 258.5 GFLOPs

backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]        # 0 P1/2
  - [-1, 1, Conv, [128, 3, 2]]       # 1 P2/4
  - [-1, 3, C2f,  [128, True]]
  - [-1, 1, CoordAtt, [128]]         # ★ CA

  - [-1, 1, Conv, [256, 3, 2]]       # 4 P3/8
  - [-1, 6, C2f,  [256, True]]
  - [-1, 1, CoordAtt, [256]]         # ★ CA

  - [-1, 1, Conv, [512, 3, 2]]       # 7 P4/16
  - [-1, 6, C2f,  [512, True]]
  - [-1, 1, CoordAtt, [512]]         # ★ CA

  - [-1, 1, Conv, [1024, 3, 2]]      # 10 P5/32
  - [-1, 3, C2f, [1024, True]]
  - [-1, 1, CoordAtt, [1024]]        # ★ CA

  - [-1, 1, SPPF, [1024, 5]]         # 13

head:
  - [6, 1, Conv, [512, 1, 1]]        # 14  proj P3
  - [9, 1, Conv, [512, 1, 1]]        # 15  proj P4
  - [13, 1, Conv, [512, 1, 1]]       # 16  proj P5

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 17 P5->P4
  - [[ -1, 15], 1, BiFPN, []] # 18
  - [-1, 3, C2f, [512]] # 19-P4/16

  - [-1, 1, nn.Upsample, [ None, 2,'nearest']] # 20 P4->P3
  - [[ -1, 14], 1, BiFPN, []] # 21
  - [-1, 3, C2f, [512]] # 22-P3/8

  - [1, 1, Conv, [512, 3, 2]] # 23 P2->P3
  - [[-1, 14, 22], 1, BiFPN, []] # 24
  - [-1, 3, C2f, [ 256 ] ] # 25-P3/8

  - [-1, 1, Conv, [512, 3, 2]] # 26 P3->P4
  - [[ -1, 15, 19], 1, BiFPN, []] # 27
  - [-1, 3, C2f, [ 512 ] ] # 28-P4/16

  - [-1, 1, Conv, [512, 3, 2]] # 29 P4->P5
  - [[-1, 16], 1, BiFPN, []] # 30
  - [-1, 3, C2f, [1024]] # 31-P5/32

  - [[25, 28, 31], 1, Detect, [nc]]
